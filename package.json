{
  "name": "momom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:core": "npm --workspace @momom/core run build",
    "build:cli": "npm --workspace @momom/cli run build",
    "build": "npm run build:core && npm run build:cli",
    "test": "vitest run",
    "test:watch": "vitest",
    "momom": "node ./packages/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
